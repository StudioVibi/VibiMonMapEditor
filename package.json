{
  "name": "vibimon-map-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outfile public/main.js --target browser && cp index.html public/index.html && mkdir -p public/styles && cp styles/main.css public/styles/main.css && cp styles/ios-theme.css public/styles/ios-theme.css",
    "server": "bun run build && bun run src/server.ts"
  }
}
